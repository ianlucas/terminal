{
  "name": "terminal",
  "version": "1.0.1",
  "description": "",
  "main": "dist/Terminal.js",
  "devDependencies": {
    "esbuild": "^0.12.0"
  },
  "scripts": {
    "start": "esbuild src/index.js --format=esm --bundle --watch --outfile=dist/Terminal.js",
    "build": "esbuild src/index.js --format=esm --bundle --minify --outfile=dist/Terminal.js"
  },
  "author": "Ian Lucas",
  "license": "MIT"
}
